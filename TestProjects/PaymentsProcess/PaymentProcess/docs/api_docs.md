# PaymentProcess API Skeleton

This document will host generated or hand-written API documentation as the
implementation matures. Keep method descriptions synchronized with the
corresponding code docstrings to avoid drift.
