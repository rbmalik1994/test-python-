# PDF component

Skeleton component for the PDF toolbox. Contains:

- `pdf.html` — partial with upload control and pages area.
- `pdf.css` — minor CSS helpers.
- `pdf.js` — lightweight JS placeholder for progressive enhancement.
